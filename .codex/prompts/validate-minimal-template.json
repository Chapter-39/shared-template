{
  "task": "Validate minimal-template for maintenance readiness",
  "instructions": [
    "Read the configuration file .codex.json in the project root and follow its rules.",
    "Verify that package.json includes scripts: format, format:check, lint, lint:fix.",
    "Check that husky and lint-staged are installed and correctly configured.",
    "Check that .github/workflows/ci.yml runs npm run lint and npm run format:check.",
    "Run npm run format:check to ensure codebase is correctly formatted.",
    "Run npm run lint to ensure codebase passes linting rules.",
    "If all checks succeed, mark the repository as ready for maintenance.",
    "Execute npm run lint to lint the code."
  ]
}
